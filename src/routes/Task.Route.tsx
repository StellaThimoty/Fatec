// import { Navigate, Outlet } from "react-router-dom"
// import { useAuth } from "../hooks/AuthProvider"

// const TaskRoute = () => {
//   const user = useAuth()
//   if (!user?.tokenstr) return <Navigate to="/login" />
//   return <Outlet />
// }

// export default TaskRoute